# A lambda function can take any number of arguments, but can only have one expression.

# This is a lambda function that adds 10 to the number passed in as an argument, and print the result:

x = lambda a : a + 10
print(x(5))
