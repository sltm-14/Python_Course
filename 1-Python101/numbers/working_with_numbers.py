import math  # Python 3 math module

""" 
    CONSTANTS:

    Python does not have constants, so,by convention, 
    we use UPPER CASE LETTERS to tell other developers that a variable will be used as a constant """

PI = 3.14159


# Python 3 Built in Functions

""" ROUND """
print(round(3.14))

""" ABSOLUTE VALUE  """
print(abs(-5))

""" FLOOR """
print(math.floor(PI))
